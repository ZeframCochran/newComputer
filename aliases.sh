alias "deleted:"="git rm "
alias "modified:"="git add "
alias "gush"="git push"
alias "gl"="git log --graph --decorate --oneline"
alias "gs"="git status"
alias "push"="git push"
alias 'gr'="echo Git Repo; basename `git rev-parse --show-toplevel`"
alias 'update'="git fetch upstream master; git merge upstream/master"
alias "docker-service"="/Applications/Docker.app/Contents/MacOS/Docker"
alias "faster"="sudo mdutil -a -i off"
alias "ll"="ls -l"
alias "edit"="code"
